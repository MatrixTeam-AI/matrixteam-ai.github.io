\section{Conclusion}

In this work, we address the instability of Classifier-Free Guidance (CFG) in large-scale models by identifying risks from excessive magnitudes during initialization. We propose \mtdbk, a training-free strategy that dynamically modulates the guidance scale based on the update-to-base ratio $r_t$. Unlike fixed scales, \mtdbk adaptively prevents artifacts during critical early steps, ensuring realistic generation trajectories. Experiments on text-to-image and text-to-video tasks demonstrate that \mtdbk significantly accelerates inference and stabilizes high-resolution generation without architectural changes. Our results highlight magnitude-aware control as a robust, efficient component for state-of-the-art foundation models.

% ======================================================

% In this work, we addressed the instability issues inherent to Classifier-Free Guidance (CFG) in large-scale generative models, identifying the practical risks associated with excessive guidance magnitudes at the initialization stage. We introduced \mtdbk, a magnitude-aware strategy that dynamically adjusts the guidance scale when the update vector becomes disproportionately large compared to the model's base prediction.

% Our approach treats CFG as \textbf{a powerful tool for alignment} that requires careful modulation rather than a static constant. Consequently, applying a fixed guidance scale often ignores the varying stability of the generation process across different samples. By grounding the modulation in the observable ratio $r_t$, we provide a robust mechanism that prevents the generation from producing artifacts during critical early steps, ensuring the trajectory remains within the realm of realistic images.

% Extensive experiments on text-to-image and text-to-video tasks demonstrate that this simple, training-free modification yields substantial practical benefits. \mtdbk enables significant inference acceleration and stabilizes high-resolution generation without requiring complex tuning or architectural changes. The robustness of our method suggests that magnitude-aware control is a key component for deploying efficient guidance strategies in state-of-the-art foundation models. Future work may explore the broader application of such adaptive schedules to other conditional generation tasks.

% ===================================================

% In this work, we addressed the instability of Classifier-Free Guidance (CFG) in large-scale generative models by introducing \mtdbk, a magnitude-aware strategy that dynamically modulates the guidance scale. We identified that excessive guidance at initialization poses practical risks, and our method mitigates this by adjusting the scale based on the update vector's magnitude ratio $r_t$. We demonstrated that this simple, training-free modification significantly accelerates inference and stabilizes high-resolution generation across both text-to-image and text-to-video tasks.
% We suggest that magnitude-aware control is essential for efficient guidance in foundation models. We plan to explore the broader application of such adaptive schedules to other conditional generation tasks in future work.